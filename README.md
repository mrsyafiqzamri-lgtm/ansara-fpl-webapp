# ANSARA FPL Ranking Extractor

Phone-friendly Streamlit app. The embedded ranking master and Excel golden template are encrypted. Configure `ASSET_KEY` and `APP_PASSWORD` only in Render environment variables.
